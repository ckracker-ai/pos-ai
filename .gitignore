# Dependencies
**/node_modules/
.pnp
.pnp.js

# Build outputs
**/.next/
**/out/
**/dist/
**/build/
*.tsbuildinfo
build-out.txt
# TypeScript emit accidentally written next to sources (BFF)
pos-api-bff/src/**/*.js

# Environment & secrets — NUNCA en Git (solo en VPS / máquina local)
.env
.env.*
**/.env
**/.env.*

# Operación local (no forma parte del producto versionado)
/deploy/
/docs/
/scripts/
SPRINT-PLAN.md
CHANGELOG.md

# IDE / agente local
.cursor/
.vscode/
.idea/
*.swp
*.swo
*~
.project

# Temp / QA local
tmp-*.json
tmp-*

# Logs & debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test coverage
**/coverage/

# OS
.DS_Store
Thumbs.db
desktop.ini

# SSL / certs (Let's Encrypt local)
nginx/certs/live/
nginx/certs/archive/
nginx/certs/accounts/
nginx/certs/renewal/
!nginx/certs/.gitignore

# Docker local overrides (optional)
docker-compose.override.yml

# MySQL data (if ever bind-mounted locally)
mysql-data/
