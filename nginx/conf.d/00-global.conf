# Límite para imágenes de mermas u otros uploads vía BFF
client_max_body_size 10M;

# ACME HTTP-01 (Let's Encrypt) — activo en HTTP y HTTPS
location ^~ /.well-known/acme-challenge/ {
    root /usr/share/nginx/html;
    default_type "text/plain";
    try_files $uri =404;
}
