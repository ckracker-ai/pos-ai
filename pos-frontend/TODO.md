# TODO
- [ ] (Debug) Insertar `alert` y `console.info` temporales para verificar qué `branchId` está activo en `/pos` y qué sucursal viaja en requests.
- [ ] Ejecutar flujo `/pos` y capturar logs: `branchId` y payload/data del endpoint.
- [ ] (Debug) Insertar `console.info` en la función de llamadas al endpoint relevante (solo la primera request que cargue datos).
- [ ] (Cleanup) Remover logs/alert temporales una vez confirmado el problema.

